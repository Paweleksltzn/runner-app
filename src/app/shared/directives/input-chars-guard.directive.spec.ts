import { InputCharsGuardDirective } from './input-chars-guard.directive';

describe('InputCharsGuardDirective', () => {
  it('should create an instance', () => {
    const directive = new InputCharsGuardDirective();
    expect(directive).toBeTruthy();
  });
});
